# Audit log — Issue #29: M7-2: Battery — a capability the catalog claimed for nine models and three drivers deliver
รัน: 2026-08-11 · branch: `feature/light-modules` · ผลลัพธ์: **APPROVED** · รอบรีวิว: **2** · commit `6876ed8`
เกิดจากของหลุด: **ใช่ บางส่วน** — `supports_battery=True` ครบ 9 รุ่นถูกลอกมาจาก v1 ตั้งแต่ M3-1 ทั้งที่ v1 เองก็ทำจริงแค่ 3 รุ่น · ธงนี้ไม่มีผู้บริโภคจนถึง M7 จึงไม่มีอะไรร้อง · #28 จงใจไม่แตะ เพื่อให้ใบนี้มีของแดงพิสูจน์ตัวเอง

## ทำอะไรไปบ้าง (ทีละขั้น)

1. **reviewer (Job A) — สร้าง prompt** (`/draft-delegation-prompt`)
   - Track: **full** — พฤติกรรมมี branch (กฎการข้าม, ความหมายของความล้มเหลว, การ gate ตามความสามารถ) + migration + API + หน้าเว็บ
   - TDD required: **yes** — ทุกเกณฑ์รับงานเป็น branch ซึ่งเป็นชนิดที่ assertion ผ่านแบบว่างเปล่าได้ถ้าเขียนทีหลังโค้ด
   - เขียน **D1–D11** เป็นการตัดสินใจล่วงหน้าพร้อมเหตุผล ทำให้ implementer ไม่ต้องเลือกเอง
   - **ไม่มี `docs-researcher` ถูก spawn** — reviewer หาเองจาก Gurux `OBISCodes.txt` และ Blue Book excerpt แล้วสรุปว่า `0.0.96.6.1.255` = "Battery charge display" ไม่ใช่ตัวนับเวลา และ**ไม่มีมาตรฐานฉบับฟรีที่ยืนยันหน่วยได้** ⇒ นั่นคือเหตุผลที่ค่าถูกเก็บดิบไม่ตีความ

2. **implementer — ลงมือ + ตรวจตัวเอง** (`/tdd` + `gurux-dlms` + `antd-ui` + `fastapi`)
   - ไฟล์: BE ใหม่ 4 (`battery.py`, `api/battery.py`, migration `0011`, + models) · BE แก้ 8 · เทสใหม่ 5 ไฟล์ · เทสแก้ 6 ไฟล์ · FE ใหม่ 1 หน้า + แก้ 3 · เอกสาร 4
   - **แดงจริงต่อพฤติกรรม**: base capability (`AttributeError`) · `read_battery_status_via` (`ImportError`) · job (`ModuleNotFoundError`) · API (`404`)
   - **ประกาศข้อจำกัดของตัวเองโดยไม่มีใครถาม**: step 3/4 มาถึงแบบเขียวเพราะ override 4 บรรทัดเกิดพร้อมคลาสแม่ ⇒ ใช้ mutation แทนการจัดฉากแดง · และ step 6 เขียนเทส 17 ตัวเป็นก้อนก่อนเขียนโมดูล ซึ่งมันบอกเองว่าใกล้กับรูปแบบที่ `/tdd` เตือนไว้ **แทนที่จะเล่าให้ดูสวยกว่าที่ทำจริง**
   - **จับโจทย์ของ Job A ที่อ้างข้อเท็จจริงผิด**: prompt สั่งให้ทำเครื่องหมายใน `SPEC.md` "แบบเดียวกับที่ #28 ทำ" · grep แล้วพบว่า **ไม่มี `#28`/`M7-1` ใน `SPEC.md` เลยสักที่** ⇒ ใช้แบบแผน `✅ … landed with issue #NN` ที่เอกสารใช้อยู่จริง (SPEC.md:313, 327, 538, 605, 749, 759, 802) แล้วรายงานว่าโจทย์อ้างผิด
   - **ทำเกินรายการที่โจทย์ให้** (ถูกต้อง): เพิ่ม `/api/battery: battery` เข้า `GATED_PREFIXES` ของ `test_feature_entitlement.py` ⇒ route sweep ครอบ router ใหม่เหมือนทุกตัว · reviewer ยืนยันว่าเป็น coverage จริง ไม่ใช่รายการประดับ (ไฟล์นั้นมี staleness guard ที่จะแดงถ้า prefix ไม่มี route จริง)
   - ผลรันเทสต์ (จริง): ก่อนแก้ `1299 passed, 16 skipped` → หลังสร้าง `1331 passed` → หลังรอบแก้ **`1335 passed, 16 skipped (47.50s)`** · `ruff` ผ่าน 178 ไฟล์ · `pnpm lint`/`build` ผ่าน · **alembic `upgrade head → downgrade -1 → upgrade head` round-trip ผ่านบน scratch DB**

3. **reviewer (Job B) — รีวิว** (`/scrutinize`)
   - รอบ 1: **CHANGES_REQUESTED** — 2 problems (`minor`) + 5 nits · ยิง mutation 6 แกน **เขียว 4** = มี 4 กฎที่ไม่มีเทสตรึงเลย
   - รอบ 2: **APPROVED** — ยิงอีก **6 variant ที่ทำลายกฎเดิมด้วยวิธีต่างกัน** เพื่อเช็คว่าเทสใหม่ไม่ได้ถูกฟิตให้ตรงกับ mutation ที่ตัวเองบอกไว้ · **แดง 5 จาก 6** · จุดสำคัญคือ V2a/V2b ที่ลบตัวกรองทีละตัว → แดงทั้งคู่ ⇒ เทสแยกแยะ**ตัวกรองแต่ละตัวอิสระจากกัน** ไม่ใช่แค่ทั้งคู่รวมกัน ซึ่งเป็นความเสี่ยง over-fitting ที่แท้จริงของเทสที่เขียนขึ้นเพื่อตอบ finding
   - ยืนยัน hash ว่าโค้ด production ไม่ถูกแตะในรอบแก้ (`battery.py` = `8783e995…` ตรงกับ manifest รอบ 1)

   - **ควรถูกจับที่ด่านไหน** (reviewer ตอบเอง):

     | # | problem | ระดับ | ด่านที่ควรจับ |
     |---|---|---|---|
     | 1 | day-guard ไม่มีใครตรึงว่าเป็น "ต่อมิเตอร์" — ลบเงื่อนไข `device_id in` แล้ว 17 เทสยังเขียว · ผลจริง: ไซต์ 20 มิเตอร์**ไม่ได้ข้อมูลเลย**พอมีตัวใดตัวหนึ่งถูกอ่านวันนั้น | minor | **Job A** — รายการเทสบังคับเขียนว่า "รันสองครั้งได้ 1 meter call" โดยไม่เคยระบุว่าต้องมี**มิเตอร์ตัวที่สอง** |
     | 2 | ตัวกรอง device ไม่มีใครตรึง และเทสที่ชื่อว่ากรองไม่ได้กรอง — ผลจริง: **มิเตอร์ที่ Pause ถูกอ่านทุกชั่วโมง** ขัดนิยาม Pause ใน `CONTEXT.md` | minor | **Job A** — D3 เขียนกฎไว้ในร้อยแก้ว ไม่บังคับเทสที่มี device ที่ถูกตัดสิทธิ์ |
     | 3 | `driver.disconnect()` ไม่มีเทสตรึง | nit | **self-check ของ implementer** — mutation sweep ครอบแต่บรรทัดที่เพิ่งเขียน ส่วนนี้อยู่เหนือขึ้นไปบรรทัดเดียว |
     | 4 | เพดาน `le=500` ไม่มีเทสตรึง | nit | **Job A** — ระบุขอบไว้พร้อมเหตุผล แต่ไม่ใส่ในรายการเทสบังคับ |
     | 5 | `test_db.py` docstring เขียนว่า "ตารางที่สิบสอง" ทั้งที่ชุดที่ assert มีสิบ | nit | **implementer** — อ่าน diff ตัวเองซ้ำก็เจอ |
     | 6 | ขาดเคส 404 / 422 ของ API | nit | **implementer** — เช่นกัน |
     | 7 | `SPEC.md:899` ใช้ถ้อยคำไม่ตรงแบบแผนเอกสาร | nit | **implementer** — เช่นกัน |
     | 8 | *(จับได้ก่อน ship ไม่นับเป็น defect)* โจทย์อ้างว่า #28 ทำเครื่องหมายใน `SPEC.md` ไว้ | — | **Job A pre-flight** — มัน grep ตรวจ**ชื่อสัญลักษณ์**แล้ว (ซึ่งช่วยจับ `test_db.py` ได้) แต่ไม่ grep ตรวจ**คำกล่าวอ้างเรื่องรูปแบบเอกสาร** |
     | 9 | guard set สร้างจาก row id แทน device id แล้วยังเขียว | nit | **Job B รอบ 1** — ทิศทางการแก้ระบุรูปสองมิเตอร์ แต่ไม่ระบุว่ามิเตอร์ตัวไหนควรถือแถวที่ seed ไว้ · **ไม่บล็อก** โค้ดที่ ship ถูกต้อง กฎถูกตรึงจาก 5 ทิศทางแล้ว |

4. **รอบแก้ (1 รอบ):** orchestrator ส่ง problems verbatim ตรงให้ implementer → implementer **ทำ mutation ของ reviewer ซ้ำกับเทส *เดิม* ก่อน** เพื่อยืนยันว่าเขียวจริง แล้วค่อยเขียนเทสใหม่ แล้วใส่ mutation กลับเพื่อดูแดง → คืนค่าจาก scratchpad + `diff` ทุกครั้ง → **nit เอาครบ 5 ไม่ข้ามสักข้อ**
   - ผลแดงของข้อ 1 **รุนแรงกว่าที่ reviewer คาด**: reviewer เขียนว่า "อ่านได้มิเตอร์เดียวต่อวัน" แต่ของจริงคือ `assert 0 == 1` — **ไม่อ่านสักตัว** · สาเหตุเดียวกัน เห็นชัดกว่า

## ต้นทุน (เวลา + output tokens ต่อสเตจ)

| สเตจ | เวลา | tool calls | subagent_tokens (สะสม) |
|---|---|---|---|
| reviewer Job A | 13.8 นาที | 62 | 213,165 |
| implementer (build) | 27.2 นาที | 201 | 421,664 |
| reviewer Job B รอบ 1 | 8.0 นาที | 19 | 288,113 |
| รอบแก้ 1 — implementer | 6.6 นาที | 40 | 455,813 |
| รอบแก้ 1 — reviewer Job B | 4.4 นาที | 5 | 305,141 |
| **รวมเวลาจริง** | **60.0 นาที** | | |

⚠️ `subagent_tokens` เป็นค่า**สะสมของ agent** ข้าม SendMessage ห้ามบวกกัน · output tokens จริงต่อสเตจ **ไม่ได้วัด**

เทียบ #28 (95.6 นาที · 37 ไฟล์): ใบนี้ **60 นาที** ตามที่คาดไว้ว่าเล็กกว่าราวครึ่ง · รอบแก้ 6.6 นาที = **24% ของรอบสร้าง**

## ✅ skill ที่เรียกจริง — ยืนยันจาก transcript (tamper-proof)

```
reviewer   agent-a244bee23ccf99a0b.jsonl
      1 "skill":"draft-delegation-prompt"
      1 "skill":"scrutinize"

implementer  agent-abc9462e2b7d46f60.jsonl
      1 "skill":"antd-ui"
      1 "skill":"fastapi"
      1 "skill":"gurux-dlms"
      1 "skill":"tdd"
```

**ไม่มีงานซ้ำซ้อน** — implementer ไม่เรียกสกิลรีวิว และ reviewer ไม่เรียกสกิลรีวิวตัวที่สองทับ `scrutinize`

⚠️ `output_file` ที่ระบบคืนตอน spawn เป็น **0 ไบต์ถาวร** — transcript จริงอยู่ที่ `~/.claude/projects/<slug>/<session>/subagents/agent-<id>.jsonl`

## Commit

`6876ed8` — `feat: Battery — the third capability the catalog claimed and no driver backed (#29)`

## วิธีตรวจซ้ำด้วยตัวเอง (audit)

```
D=~/.claude/projects/C--Users-HP-Documents-Work-arichds-application-v2/c36a49e7-7c61-4e55-be90-6cd16b5a941a/subagents
grep -o '"skill":"[a-z0-9:_-]*"' $D/agent-a244bee23ccf99a0b.jsonl | sort | uniq -c   # reviewer
grep -o '"skill":"[a-z0-9:_-]*"' $D/agent-abc9462e2b7d46f60.jsonl | sort | uniq -c   # implementer
grep -c '"name":"Skill"' $D/agent-abc9462e2b7d46f60.jsonl
```

## ของที่ค้าง

**อ่านจากมิเตอร์จริง** — เกณฑ์เดียวที่ปิดไม่ได้ในไปป์ไลน์ · CEWE ทั้งสามตัวต่อได้จากเครื่องนี้จริง แต่ batch รันแบบไม่มีคนเฝ้าและปลายทางเป็นมิเตอร์ที่ไซต์ลูกค้า ⇒ D11 สั่งห้ามเปิด socket · implementer ยืนยันว่าไม่ได้เปิดและไม่ได้สร้าง hardware mock มาอ้างว่าเป็นการอ่านจริง

## บทเรียนที่ส่งต่อให้ #30 และ #31

reviewer วินิจฉัยสาเหตุร่วมของความผิดตัวเอง (4 ข้อใน #28 + 3 ข้อในใบนี้) แล้วเขียนเป็นคำสั่งส่งต่อ:

> เขียนเทสบังคับทุกข้อในโจทย์เป็น **input ที่ทำให้มันแดงได้** ไม่ใช่เป็น**หัวข้อที่มันครอบคลุม** — ระบุมิเตอร์ตัวที่สอง, แถวที่ถูกตัดสิทธิ์, ค่าที่อยู่อีกฝั่งของขอบเขต · ใน #29 โจทย์เขียนว่า "รัน job สองครั้งได้ 1 meter call" แล้ว implementer ก็สร้างตามนั้นเป๊ะ — มิเตอร์ตัวเดียว ซึ่งแยกกฎ "ต่อมิเตอร์" ออกจาก "ทั้งระบบ" ไม่ได้ ⇒ เกณฑ์รับงานหลักของใบ ship ออกไปพร้อมเทสที่ยังเขียวตอนลบกฎทิ้ง และการตกหล่นแบบเดียวกันสร้าง blocker ตัวที่สองที่ตัวกรอง device · แล้วตอนรีวิว **ให้ยิง mutation ทุกกฎด้วยตัวเอง แล้วถือว่าเทสที่ยังเขียวคือ finding** — และยิงกฎที่**โจทย์ของตัวเองเป็นคนตั้ง**ก่อน เพราะเทสที่สร้างขึ้นให้ตรงกับถ้อยคำในโจทย์ คือตัวที่มีโอกาสสูงสุดที่จะมีรูปร่างตามถ้อยคำ ไม่ใช่ตามกฎ

⇒ แปะเข้าโจทย์ Job A ของ #30 และ #31 แล้ว

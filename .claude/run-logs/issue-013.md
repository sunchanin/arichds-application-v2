# Audit log — Issue 013: จองคีย์ `file_upload_destination` ก่อนออก licence ใบแรก

รัน: 2026-08-26 · branch: `feature/light-modules` · ผลลัพธ์: **APPROVED** · รอบรีวิว: **2**
เกิดจากของหลุด: **ไม่ใช่** — เจ้าของถามเองว่า "เพิ่ม file upload เข้าไปใน feature ของ license ได้ไหม"
ระหว่างทดสอบการจำกัดฟีเจอร์

## ทำอะไรไปบ้าง (ทีละขั้น)

### 1. reviewer (Job A) — สร้าง prompt (`/draft-delegation-prompt`)
- **Track: full** — เป็นข้อมูลบรรทัดเดียว แต่ลงบน contract ข้ามภาษาที่มี tripwire สามตัว, พื้นผิวที่ลูกค้าเห็น,
  และการตัดสินใจที่คนอ่านทีหลัง**มีแนวโน้มจะไป "แก้ให้ถูก"**
- **TDD required: yes** — มี red จริงสามจังหวะที่คนละที่กัน และ red คือหลักฐานเดียวว่า tripwire ยังมีชีวิต
- **Workflow discipline: ไม่มี domain skill ไหนเกี่ยวข้อง** — พิจารณาทั้งสามตัวแล้วตัดออกทีละตัวพร้อมเหตุผล

**สิ่งที่ reviewer ทำก่อนเขียนโจทย์ และคุ้มที่สุด — กวาดหาจุดที่นับ "สิบคีย์" ไว้ก่อน** เจอ 4 จุดที่ตัว
issue ไม่ได้ระบุ (`test_dataout_config_api.py:37` · `web/src/api.ts:38` · `features.ts:148` ·
`test_feature_entitlement.py:292`) และ**เจอคำอ้างอิงที่ผิดอยู่แล้ว**ใน `constants.py:307` —
อ้าง `SPEC.md:1050-1053` สำหรับประโยค "ยังไม่เคยออก licence" แต่บรรทัดนั้นเป็นเรื่อง watermark ของ §3.8
· สั่งให้แก้เป็น **section anchor `SPEC.md §3.9` ไม่ใช่เลขบรรทัดใหม่** เพราะการแก้ SPEC ในขั้นตอนถัดไป
จะแทรกบรรทัดแล้วทำให้เลขที่เพิ่งเขียนเก่าทันทีในวันเดียวกัน

**D1 — mapping ยังเป็น `"never"` ไม่เปลี่ยน** · issue 012 ให้เหตุผลไว้สองข้อ (ไม่มีคีย์ + ไม่มีอะไรหลังหน้า)
· 013 ลบข้อแรกทิ้ง ข้อสองยังอยู่และเป็นข้อที่รับน้ำหนัก · **ยอมรับผลที่ตามมาแทนที่จะซ่อน:** ระหว่างนี้ถึง M8
รีโปจะมีคีย์ที่ขายได้แต่ไม่ได้กั้นอะไรเลย โดยตั้งใจ — และนั่นคือสิ่งที่คนอ่านทีหลังจะอ่านผิดว่าเป็นบั๊ก

**D4 — ตรงไหนที่ตัวเลขเป็นของแถม ให้ลบตัวเลข ไม่ใช่เพิ่มเลข** · ประโยคอย่าง "the ten sellable keys are
deliberately not duplicated in TypeScript" ใจความอยู่ที่*ไม่ทำสำเนา* — ลบเลขออกแล้วมันเก่าไม่ได้อีก

### 2. implementer — ลงมือ + ตรวจตัวเอง (`/tdd`)
- ไฟล์: `constants.py` · `features.ts` · `AppShell.tsx` · `api.ts` · `SPEC.md` §3.9 + เทสสี่ไฟล์
- **ปฏิเสธที่จะแกล้งทำ red** — เทสใหม่มีสองครึ่ง ครึ่งแรกแดงจริง แต่**ครึ่งหลัง (คีย์ต้องไม่กั้นหน้าไหน)
  เขียวตั้งแต่แรก** เพราะคุมสภาพที่ถูกอยู่แล้ว · มันเขียนว่า *"red ตรงนั้นเกิดได้ทางเดียวคือ mutation
  ที่ D1 ห้าม ผมจึงไปเอา red นั้นมาในลูป แทนที่จะเคลม red ที่ผมไม่มี"*
- **ข้อความ error ที่เขียนสำหรับคนแปลกหน้าในอนาคต** ระบุทั้งสิ่งที่ผิด เหตุผล และเมื่อไหร่ถึงเปลี่ยนได้:
  *"Reserving the key did not build the feature (D1, issue 013): there is no endpoint, no settings row
  and no job … Leave PAGE_ENTITLEMENT at { kind: "never" } until M8 / SPEC §3.8 gives the page a transport."*
- **เจอจุดที่โจทย์ไม่ได้ระบุแล้วเลือกไม่แก้:** `tools/arichds_vendor.py:226` เขียนว่า "a nine-key world" —
  เป็นข้อความ*ประวัติศาสตร์*ที่ยังจริง การแก้จะทำให้มันเท็จ · รายงานแทนที่จะแก้เงียบๆ
- **รายงานว่า 3 ใน 7 mutation ยังเขียว** (D2/D3/D4) — เหตุผลของการจองทั้งหมดอยู่ในคอมเมนต์กับ SPEC
  ซึ่งไม่มีอะไรบังคับ · เลือกรายงานตรงๆ แทนที่จะเขียนเทส assert คอมเมนต์ *"ซึ่งจะแย่กว่าช่องว่างเสียอีก"*
- **หลักฐาน render** ผ่าน vite SSR pipeline ด้วย virtual entry (ไม่สร้างไฟล์ใน `web/`): license ที่ให้สิทธิ์
  ครบ 11 คีย์รวม `file_upload_destination` → เมนูมี **Database** ไม่มี **File Upload**
- ผลรันเทสต์: **1851 → 1854 passed**

### 3. reviewer (Job B) — รีวิว (`/scrutinize`)
- verdict: **CHANGES_REQUESTED** — 1 minor + 3 nit · **minor เป็น defect ของโจทย์ที่ reviewer เขียนเอง**

**minor — เราสร้างการ์ดไว้ผิดฝั่ง**

> ฝั่งสินค้ามีการ์ด 5 ตัวกันไม่ให้หน้าโผล่ · **ฝั่งการขายมีศูนย์** และ CLI ยังแสดงคีย์นี้ในรายการ
> "ชื่อฟีเจอร์ที่ใช้ได้" อย่างเป็นทางการ

ประโยคบอกอันตรายของ issue เองคือ *"ลูกค้าที่ซื้อคีย์นี้วันนี้ กำลังซื้อฟอร์มที่ทิ้งสิ่งที่เขาพิมพ์"* —
และ**ทางเดียวที่ลูกค้าจะซื้อได้คือผ่าน CLI ตัวนี้** · reviewer วัดมา: `_feature_errors(['file_upload_destination'])`
ผ่านเงียบ, `grep -i "reserved" tools/arichds_vendor.py` ไม่เจอ · **เป็นรูปแบบเดียวกับที่ issue นี้มีไว้ป้องกัน**
— การตัดสินใจที่บันทึกเป็นร้อยแก้วเท่านั้น ณ ที่ที่คนตัดสินใจไม่ได้อ่าน

- **reviewer ยกข้อห้ามของตัวเอง**ให้แตะ `tools/` ได้ พร้อมเงื่อนไขสามข้อ: ต้อง**เตือน ไม่ใช่ปฏิเสธ**
  (เกณฑ์ข้อ 3 บังคับว่าคีย์ต้องเซ็นได้) · ห้ามกรองการ์ดฝั่งลูกค้า (D2 ยังยืน) · **ห้ามพิมพ์ชื่อคีย์ลงใน
  `tools/` ตรงๆ** เพราะ 010 ตัดสินว่า `tools/` ต้อง import ไม่ทำสำเนา
- **แก้ตัวเลขของ implementer:** mutation D1 ทำให้แดง **5 ไม่ใช่ 7**

**ควรถูกจับที่ด่านไหน (ต่อ problem):**
- minor (CLI ขายคีย์ที่จองไว้เงียบๆ) — **โจทย์ Job A** · Out-of-scope ของมันห้ามแตะ `tools/` และ D2 อ้าง
  ว่า "คนขายคุมได้อยู่แล้วด้วยการระบุ `--features`" โดย**ไม่เคยกำหนดว่าการควบคุมนั้นต้องได้รับข้อมูล**
- nit 2 (ตัวเลข 7 vs 5) — **self-check ของ implementer** ถ้าเทียบกับจำนวนเทสที่ระบุชื่อ
- nit 3 (emoji ตัวแรกใน `app/src/**/*.py`) — Job B เท่านั้น
- nit 4 (ค่าใน `FEATURE_LABELS` ผิดแล้วไม่มีอะไรจับ) — **ของเก่าจาก 012** ไม่ใช่ของใบนี้

### 4. รอบแก้ (1 รอบ)
- `RESERVED_FEATURE_KEYS` วางไว้ข้าง `SELLABLE_FEATURE_KEYS` · `tools/` import แบบ lazy เหมือนเดิม
  **ไม่มีชื่อคีย์เป็น literal ใน `tools/` เลย** พิสูจน์ด้วยเทสที่ grep ซอร์สของ CLI เองหาสมาชิกทุกตัวของเซ็ต
  ⇒ **M8 คือการแก้จุดเดียว (ทำเซ็ตให้ว่าง) และทำครึ่งๆ ไม่ได้**
- **implementer แก้คำอธิบายของ reviewer ด้วย** — reviewer เดาว่าเลข 7 มาจากบรรทัดซ้ำของ `-n auto` ·
  ที่จริงเป็น scoped run ที่มี 7 test ID จริง แต่ถ่ายจากตอนที่**ยังไม่ได้เพิ่มคีย์** สองตัวจึงมาจากคีย์ที่หายไป
  · *"รายงานผมปนสถานะ tree สองอัน เนื้อหาถูก แต่การระบุที่มาผิด"*
- **สามอย่างที่ self-check เจอ:** ลดพารามิเตอร์ `reserved` จากมี default เป็น**บังคับ** (*"ผู้เรียกคนเดียว
  ความยืดหยุ่นนี้ทำได้อย่างเดียวคือทำให้ผู้เรียกอนาคตทิ้ง marker เงียบๆ"*) · **เจอ em dash เพี้ยนเป็น `?`
  เพราะรันจริง** ในข้อความเดียวที่เขียนถึงคนขาย · และเจอกับดักที่ **010 ทิ้งไว้เอง**
- mutation ที่ reviewer สั่ง — สามทางเข้า แดงทั้งสาม · ทางที่สาม (ทำเซ็ตให้ว่าง) **แดง 2 ที่เหลือเขียว 96**
  ⇒ CLI ถอยกลับไปเป็นพฤติกรรมก่อนแก้พอดี **จึงเป็นหลักฐานว่าการเลิกจองตอน M8 ปลอดภัย**
- ผลรันเทสต์: **1854 → 1862 passed**

### 5. reviewer (Job B รอบ 2) — **APPROVED**
- ยิง CLI จริง 4 แบบ ยืนยันว่า **stderr คือช่องทางคุยกับคนของเครื่องมือนี้อยู่แล้ว** (สรุป Customer /
  Machine ID ก็ไปที่นั่น) ส่วน stdout มีแต่โค้ด base64 ⇒ คำเตือนไปลงในกระแสที่คนขายอ่านอยู่แล้ว
  และ pipe ยังสะอาด · mutation ของมันเอง (ย้ายคำเตือนไป stdout) **แดง 3 ที่** รวมเทสเก่าที่ decode payload
- ยืนยันเหตุผลของ "ไม่เตือนเมื่อไม่ระบุ `--features`": *อันตรายคือลูกค้า **ซื้อ** คีย์นี้ และการซื้อเกิดจาก
  การที่ใครสักคนไปใส่มันในใบรายการ — คุณใส่สิ่งที่คุณพิมพ์* · license แบบ `null` ไม่ได้ระบุอะไรเลย
- ตอบเรื่องเวลาเทสที่เพิ่มขึ้น: **รันเองได้ 153.59s ช้ากว่า implementer 23 วินาทีบน tree เดียวกันเป๊ะ**
  ⇒ เป็นภาระเครื่อง ไม่ใช่ของที่ใบนี้เพิ่ม · `--durations=8` ไม่มีเทสใหม่ติดเลยสักตัว
- **แก้การระบุสาเหตุของ implementer อีกที:** em dash (U+2014) แทนได้ใน cp1252 (0x97) · ตัวที่พังจริงคือ
  `─` box-drawing ในคอมเมนต์ · **การกระทำถูกอยู่แล้วไม่ว่าเหตุผลจะเป็นอะไร**

## ต้นทุน (เวลา + tool calls + tokens)

| สเตจ | เวลา | tool calls | tokens (ดูหมายเหตุ) |
|---|---|---|---|
| reviewer Job A | 6.8 นาที | 23 | 114,404 |
| implementer (build) | 16.6 นาที | 62 | 158,048 |
| reviewer Job B | 8.5 นาที | 14 | 160,479 |
| รอบแก้ 1 — implementer | 18.0 นาที | 28 | 206,624 |
| รอบแก้ 1 — reviewer Job B | 9.5 นาที | 12 | 189,965 |

**หมายเหตุ tokens:** `subagent_tokens` บวกกันตรงๆ ไม่ได้ — double-count ข้าม SendMessage resume

## ✅ skill ที่เรียกจริง — ยืนยันจาก transcript (tamper-proof)

```
=== IMPLEMENTER (964K) ===
      1 "skill":"tdd"
Skill calls: 1

=== REVIEWER (760K) ===
      1 "skill":"draft-delegation-prompt"
      1 "skill":"scrutinize"
Skill calls: 2
```

**สะอาด** — implementer เรียก `tdd` ตัวเดียวตามที่โจทย์บังคับ (reviewer ตัดสินว่าไม่มี domain skill ไหน
เกี่ยว จึงถูกต้องที่ไม่มีตัวอื่น) และไม่ได้เรียกสกิลรีวิวมาตรวจงานตัวเอง · ไม่มีใคร spawn `docs-researcher`

## Commit

`049c0b9` — **feat: reserve a sellable key for the File Upload Destination (issue 013)**

## ของที่ยังค้าง (ไม่บล็อก เจ้าของตัดสิน)

1. **`sign --features ""` ให้ `features: None` เงียบๆ = เปิดทุกคีย์** — ทั้งที่ `None` กับ `[]` ถูก
   ออกแบบให้ตรงข้ามกัน (`features.py:47`) · **สตริงว่างจึงให้ license ที่กว้างที่สุด** · ทั้ง implementer
   และ reviewer ไล่ `git log -S` ตรงกันว่ามาจาก `77ccf8a` (issue 010) จึงอยู่นอกขอบเขตแม้แต่ที่ถูกยกให้ ·
   **ควรมี issue ของตัวเอง** — กับดักที่บันทึกไว้เฉยๆ ไม่มีผลบังคับ คือสิ่งที่ใบ 013 นี้เถียงมาทั้งใบ
2. **ค่าใน `FEATURE_LABELS` ที่ผิดไม่มีอะไรจับ** — reviewer ตั้ง `file_upload_destination:
   "Database Destination"` แล้วเทสเขียวหมด ลูกค้าจะเห็นแท็กชื่อซ้ำกันสองอัน · tripwire ตรวจแค่ว่า*มี*ป้าย
   และป้ายไม่ได้ชี้คีย์ที่ตายแล้ว **ไม่มีตัวไหนอ่านข้อความ** · ของเก่าจาก 012
3. **เทสช้าลงจาก 012** — `test_a_licence_granting_only_that_key_is_not_refused` วนเก้าหน้า แต่ละเคส
   relicense ผ่าน Ed25519 จริง · เป็นที่แรกที่ควรดูถ้าต้องลดเวลา gate

## วิธีตรวจซ้ำด้วยตัวเอง (audit)

    d="C:/Users/HP/.claude/projects/C--Users-HP-Documents-Work-arichds-application-v2/c36a49e7-7c61-4e55-be90-6cd16b5a941a/subagents"
    grep -o '"skill":"[a-z0-9:_-]*"' "$d/agent-ad9070474f64bff7d.jsonl" | sort | uniq -c   # implementer
    grep -o '"skill":"[a-z0-9:_-]*"' "$d/agent-af0dbeb2418f9494f.jsonl" | sort | uniq -c   # reviewer
    grep -c '"name":"Skill"' "$d/agent-ad9070474f64bff7d.jsonl"

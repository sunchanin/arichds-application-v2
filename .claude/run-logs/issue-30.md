# Audit log — Issue #30: M7-3: Export Format, and the Load Profile CSV that appends under it
รัน: 2026-08-11 · branch: `feature/light-modules` · ผลลัพธ์: **APPROVED** · รอบรีวิว: **2** · commit `eb0cbeb`
เกิดจากของหลุด: **ไม่ใช่ งานใหม่** — แต่เป็นหนี้ที่ค้างมาตั้งแต่ grill M5 (2026-08-07) ที่ย้าย CSV export จาก M5c มา M7 ⇒ ลูกค้าไม่มี CSV ตั้งแต่ M5 ถึงตอนนี้

## ทำอะไรไปบ้าง (ทีละขั้น)

1. **reviewer (Job A) — สร้าง prompt** (`/draft-delegation-prompt`)
   - Track: **full** · TDD required: **yes** — exporter เป็น branch logic แทบทั้งตัว (skew cap 4 กิ่ง, watermark มีขอบ, fsync มีเส้นทางล้มเหลว)
   - Workflow discipline: `antd-ui` + `fastapi` · **ตัด `gurux-dlms` ออกถูกต้อง** — ใบนี้ไม่แตะ DLMS เลย
   - **นี่คือใบแรกที่ใช้บทเรียนจาก #29** — เทสบังคับ 15 ข้อ (T1–T15) **ทุกข้อระบุ input พร้อมเหตุผลว่าทำไม input ที่อ่อนกว่าถึงผ่านทั้งที่ลบกฎทิ้ง** เช่น T2 สั่งใช้ค่าพลังงานไม่เป็นศูนย์และต่างกันทุกช่อง *เพราะศูนย์ทำให้การคูณ 1000 ไม่มีผลแล้วเทสกลายเป็นเทสเปล่า* · T12 สั่งให้มี device ปกติอยู่ด้วยอีกตัว *เพราะถ้ามีตัวเดียว การ `return` ทิ้งทั้ง cycle ก็ผ่าน*
   - **ไม่มี `docs-researcher` ถูก spawn** — reviewer grep v1 เองแล้วพิสูจน์ว่า `output_csv` มีผู้อ่านตัวเดียวคือ `output_txt and not output_csv`

2. **implementer — ลงมือ + ตรวจตัวเอง** (`/tdd` + `antd-ui` + `fastapi`)
   - ไฟล์ใหม่ 7 (`db/load_profile_query.py`, `export/{__init__,format,csv_export}.py`, migration `0012`, `ExportFormat.tsx`, + เทส 6 ไฟล์) · แก้ 13 · **เทสใหม่ 69 ตัว**
   - **ลำดับงานถูกต้อง**: ดึงโค้ดรวม Logger 1/2 ออกมาก่อน แล้วพิสูจน์ว่าเทสเดิมของหน้า Load Profile (20 ตัว) ผ่าน**โดยไม่แก้ assertion สักตัว** ก่อนจะสร้างอะไรทับ
   - **แหกคำสั่งหนึ่งจุดโดยประกาศ**: คืน `CsvExportResult(rows_written, path)` แทน `-> int` เพราะ API ต้องใช้ path และการคำนวณ path ซ้ำที่ชั้น API จะ drift ⇒ **reviewer รับ** (โจทย์เขียนว่า "suggested surface — settle on it" ซึ่งเป็นการเชิญ ไม่ใช่คำสั่ง)
   - **รายงาน TDD ตามจริงว่า "mixed"**: แดงสองตัวแรกเป็น missing-symbol ซึ่ง**มันบอกเองว่าไม่พิสูจน์อะไร** แล้วไปยิง mutation จริงแทน 5 ครั้ง (สลับลำดับ coalesce · ตั้ง ICT offset เป็น 0 · skew cap `>=`→`>` · watermark `>`→`>=` · ลบ try/except `build_driver`) คืนค่าและตรวจ sha256 ทุกครั้ง
   - หลักฐานเพิ่มที่ยังไม่เคยมีใครยกมาก่อนในไปป์ไลน์นี้: **ชุดเทสที่เพิ่งเขียนจับบั๊กในตัวเทสเองได้ 2 ตัวทันทีที่รัน** (ตั้ง skew cap ผิด + `tmp_path` ชนกับโฟลเดอร์ฐานข้อมูล) ⇒ สัญญาณว่าเทสไม่ได้ถูกเขียนให้เขียวตามโค้ด
   - ⚠️ **รายงานผิดหนึ่งจุด**: อ้างว่าจับ baseline ไม่ได้ "เพราะ tree มีของค้างจากงานอื่น" — **ไม่จริง** tree สะอาดตอนเริ่ม (#29 commit ไปแล้ว) · orchestrator ตรวจเลขเอง `1335 + 69 = 1404` ตรงพอดี และ reviewer ยืนยันว่าไม่มีไฟล์เทสถูกลบ ⇒ **เป็นการรายงานผิด ไม่ใช่การซ่อน regression**
   - ผลรันเทสต์: `1404 passed` → หลังรอบแก้ **`1409 passed, 16 skipped (79.32s)`** · ruff 189 ไฟล์ผ่าน · `pnpm lint`/`build` ผ่าน · alembic round-trip ผ่าน

3. **reviewer (Job B) — รีวิว** (`/scrutinize`)
   - รอบ 1: **CHANGES_REQUESTED** — 2 problems (`minor`) · ยิง **13 แกน แดง 11**
   - รอบ 2: **APPROVED** — ยิงกับโค้ดที่แก้แล้ว 4 แกน **แดงทั้งหมด** · เลขเทสตรง `1404 + 5 = 1409` (เทส lock ถูก**เขียนใหม่ ไม่ใช่เพิ่ม**)
   - ยืนยันของที่อาจพลาดได้: การเปลี่ยน `total` ที่มากับการดึงโค้ดออก **ปลอดภัย** เพราะ `UniqueConstraint("device_id","logger_id","read_at")` ทำให้ outer join เป็น 1:1 อย่างมาก ⇒ สัญญาของ endpoint ไม่เปลี่ยน

   - **ควรถูกจับที่ด่านไหน** (reviewer ตอบเอง):

     | # | problem | ระดับ | ด่านที่ควรจับ |
     |---|---|---|---|
     | 1 | เทส lock เดียวกัน**แดงไม่ได้เลย** — ตัวเขียนที่ถูก patch บล็อกทั้งสองเธรด ทำให้ `is_alive()`/elapsed จริงทั้งสองแบบ | minor | **Job A** — T13 ระบุ*สถานการณ์* ไม่ใช่*ตัวสังเกตที่แยกแยะได้* |
     | 2a | `status != OFFLINE` กันแถวที่เก็บไว้แล้วไม่ให้ถึงไฟล์ CSV ของลูกค้า อย่างเงียบและไม่มีกำหนด | minor | **Job A** — D-10 ระบุจังหวะและตำแหน่งใน registry แต่ไม่เคยระบุว่า job เดินผ่าน device ชุดไหน |
     | 2b | ตัวกรองทั้งสองไม่มีเทสตรึง — ลบ `.where(...)` ทิ้งทั้งก้อนแล้ว 22 เทสยังเขียว | minor | **self-check ของ implementer** — mutation sweep ครอบเส้นทาง export แต่ไม่ครอบ query ของ cycle |
     | 3 | docstring อ้างว่ามีการตรวจ non-empty ที่ไม่มีจริง | nit | **implementer** — ข้อความกับโค้ดขัดกันในฟังก์ชันเดียว |
     | 4 | "Leave empty to disable auto-save" ขัดกับ 422 ของ API | nit | **implementer** — endpoint ที่มันเรียกอยู่ใน diff เดียวกัน |
     | 5 | สองแท็บเซฟทับกัน | nit | **ไม่มีใคร** — ประกาศถูกต้อง เลื่อนถูกต้อง เป็นรูปแบบที่ `capture_dir` มีอยู่แล้ว |
     | 6 | ตัวนับอยู่ที่ operation ในสุด ⇒ ตรึงว่า "มี lock" แต่ไม่ตรึงว่า "lock คลุมทั้ง critical section" | nit | **Job B รอบ 1** — ทิศทางการแก้ระบุ*ค่า*ของตัวนับ แต่ไม่ระบุ*ตำแหน่ง* · **ไม่บล็อก โค้ดที่ ship ถูกต้อง** → เปิดเป็น `docs/issues/003-*.md` |

   > **สี่ใน six ปัญหาจริงย้อนไปที่ถ้อยคำของ Job A หรือ Job B ไม่ใช่ที่ implementer** — ทั้งสองรอบมันสร้างตรงตามที่ระบุเป๊ะ และทั้งสองครั้งมันชูคำถามที่ยังไม่มีใครตัดสิน (ตัวกรอง device) แทนที่จะเดาเงียบ ๆ

4. **รอบแก้ (1 รอบ):** implementer **ทำ mutation ของ reviewer ซ้ำกับโค้ด*ก่อนแก้*ก่อน** เพื่อยืนยันว่าเขียวจริง แล้วค่อยแก้ แล้วยิงซ้ำเพื่อดูแดง → คืนค่าและ hash ทุกครั้ง
   - **ทำเกินที่สั่งหนึ่งจุด**: nit 3 สั่งแค่แก้ docstring · มันไป**เพิ่มการตรวจสอบจริง** (`_validate_date_format` 422) เพราะรูปแบบวันที่ว่างจะทำให้คอลัมน์ Date/Time ของทุกไฟล์ที่ส่งออกต่อจากนี้กลายเป็นช่องว่างเงียบ ๆ ⇒ **reviewer รับ** โดยชี้ว่า finding เสนอไว้สองทางเลือกอยู่แล้ว การเลือกทางที่แข็งกว่าคือการทำตาม ไม่ใช่ scope creep

## ต้นทุน (เวลา + output tokens ต่อสเตจ)

| สเตจ | เวลา | tool calls | subagent_tokens (สะสม) |
|---|---|---|---|
| reviewer Job A | 12.6 นาที | 56 | 152,369 |
| implementer (build) | **43.0 นาที** | 219 | 482,543 |
| reviewer Job B รอบ 1 | 9.1 นาที | 28 | 239,613 |
| รอบแก้ 1 — implementer | 10.8 นาที | 49 | 513,442 |
| รอบแก้ 1 — reviewer Job B | 4.0 นาที | 7 | 257,924 |
| **รวมเวลาจริง** | **79.5 นาที** | | |

⚠️ `subagent_tokens` เป็นค่าสะสมของ agent ข้าม SendMessage — ห้ามบวกกัน · output tokens จริงต่อสเตจ **ไม่ได้วัด**

## ✅ skill ที่เรียกจริง — ยืนยันจาก transcript (tamper-proof)

```
reviewer   agent-a2b60f04bdeed34de.jsonl  (720K)
      1 "skill":"draft-delegation-prompt"
      1 "skill":"scrutinize"

implementer  agent-a518f90590e566946.jsonl  (2.7M)
      1 "skill":"antd-ui"
      1 "skill":"fastapi"
      1 "skill":"tdd"
```

**ไม่มีงานซ้ำซ้อน** · `gurux-dlms` ไม่ถูกเรียก ซึ่ง**ถูกต้อง** — Job A ไม่ได้สั่ง เพราะใบนี้ไม่แตะ DLMS

## Commit

`eb0cbeb` — `feat: Export Format, and the Load Profile CSV that appends under it (#30)`

## วิธีตรวจซ้ำด้วยตัวเอง (audit)

```
D=~/.claude/projects/C--Users-HP-Documents-Work-arichds-application-v2/c36a49e7-7c61-4e55-be90-6cd16b5a941a/subagents
grep -o '"skill":"[a-z0-9:_-]*"' $D/agent-a2b60f04bdeed34de.jsonl | sort | uniq -c   # reviewer
grep -o '"skill":"[a-z0-9:_-]*"' $D/agent-a518f90590e566946.jsonl | sort | uniq -c   # implementer
```

## ของที่ค้าง

- **อ่านจากมิเตอร์ CEWE จริง** — owner-deferred เหมือน #28/#29
- **`output_txt` ยังเป็นคำถามค้าง** ไม่ใช่การตัดสิน — เจ้าของต้องถามลูกค้าว่ามีไซต์ไหนเปิดใช้ไหมก่อนตัดถาวร
- **`docs/issues/003`** — เทส lock ตรึงการมีอยู่ ไม่ตรึงขอบเขต

## บทเรียนที่ส่งต่อ — วัดผลได้แล้ว

| ใบ | finding ที่เป็น "เทสจับไม่ได้" |
|---|---|
| #28 | 5 จาก 6 |
| #29 | 2 blocker ทั้งคู่ |
| **#30** | **1 จาก ~15 พฤติกรรม** · probe 11/13 แดงตั้งแต่ยิงครั้งแรก |

กลไกที่ทำให้ต่าง คือ**ประโยคที่ระบุ input** — ทุกจุดที่โจทย์ระบุ input ไว้ (แถวคร่อมเส้น logger 2 · เคส 24 ชม.เป๊ะ · device ตัวที่สองที่ต้องไม่รั่ว · แถวตรง watermark พอดี · ค่าพลังงานสี่ตัวที่ต่างกัน · เวลาข้ามเที่ยงคืนหลังเลื่อน +7) mutation แดงหมด

> *มันไม่ใช่เทสที่บังเอิญดี — มันคือเทสที่เขียนให้อ่อนไม่ได้ เพราะ input บังคับอยู่*

**จุดบอดสองข้อที่ใบนี้เจอ และส่งต่อให้ #31 แล้ว:**

1. **ระบุ "ตัวสังเกตที่ค่าต่างกัน" ไม่ใช่ "สถานการณ์"** — *"ถือ lock ไว้แล้วยืนยันว่าตัวที่สองยังไม่ทำงาน"* อ่านดูเหมือนเทสแต่ไม่ตรึงอะไร เพราะ "ยังไม่เสร็จ" จริงทั้งสองแบบ · *"ยืนยันว่ามีไม่เกินหนึ่งเธรดอยู่ในการเขียนพร้อมกัน"* คือสถานการณ์เดียวกันที่มีตัวเลขติดมา และเป็นเวอร์ชันที่แดงเมื่อลบกฎ · **corollary เรื่องตำแหน่งตัวนับ ไม่ส่งต่อให้ #31** เพราะตัวอ่านไฟล์ log ไม่มี critical section ให้เกาะ และกฎที่แปะผิดที่จะสอนให้คนอ่านข้ามกฎถัดไป
2. **เกณฑ์แบบ "X เปลี่ยนแล้ว Y ไม่เปลี่ยน" ตาบอดต่อความผิดที่เกิดกับทั้งสองรอบ** — reviewer หารพลังงานด้วย 1000 ทุกเซลล์แบบไม่มีเงื่อนไข แล้ว T2 ("สลับ kW/W ต้องไม่มีไบต์เปลี่ยน") **ยังเขียว** · ตัวที่จับได้คือเทสยืนยันไบต์ทั้งไฟล์ ซึ่ง implementer เขียนเองโดยไม่มีใครสั่ง ⇒ **เกณฑ์หัวใจของ ADR 0013 รอดมาได้เพราะโชค** · ข้อนี้ใช้กับ #31 **แรงกว่าที่ใช้กับใบนี้อีก** เพราะเกณฑ์ตามธรรมชาติของตัวอ่าน log เป็น invariance แทบทั้งหมด (redaction ต้องไม่แตะอย่างอื่นบนบรรทัด · ตัวกรองระดับต้องไม่สลับลำดับหรือทำหาย)

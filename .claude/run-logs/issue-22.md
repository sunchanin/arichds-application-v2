# Issue #22 — M6b: สิทธิ์ขายแยก · ตาราง settings · capture PDF/xlsx

**สถานะ: DONE** · commit `e8fef84` · branch `feature/billing` · 45 ไฟล์ · +3,315/−64
**รอบรีวิว: 2** (CHANGES_REQUESTED → APPROVED)

---

## ขั้นตอนที่เดินจริง

1. **Job A** — reviewer อ่าน issue + SPEC §3.6/§3.9 + ADR 0010 + lib-notes แล้วร่าง DELEGATION PROMPT
   Track **full** · TDD **บังคับ** · mandate 2 สกิล (`fastapi` · `antd-ui`) · **สั่งห้าม `gurux-dlms` ชัดเจน** เพราะ issue นี้ไม่แตะ driver
2. **Implement** — ~56 นาที · แบ่งสามเฟส เทสเต็มเขียวคั่นทุกเฟส (888 → 922 → 968 → 970)
3. **Job B รอบ 1** — CHANGES_REQUESTED · บล็อก 3 ข้อ + nit 4 ข้อ · mutation probe **14 ตัว (แดง 12 เขียว 2)**
4. **Fix รอบ 1** — แก้ครบ 3 ข้อ · รับนิต 3 จาก 4
5. **Job B รอบ 2** — APPROVED · **mutation ชุดใหม่ 6 ตัวที่ตั้งใจให้ต่างจากรอบแรก** · แดงทั้งหมด
6. **Commit** — `e8fef84`

---

## สกิลที่ถูกเรียกจริง (ตรวจจาก transcript)

| agent | สกิล | output tokens |
|---|---|---|
| reviewer | `draft-delegation-prompt` (Job A) · `scrutinize` (Job B) | 146,513 |
| implementer | `tdd` · `fastapi` · `antd-ui` | 305,369 |

**mandate ครบ** · **ไม่มีรีวิวซ้ำซ้อน**

> ⚠️ **ความคลาดเคลื่อนในทิศทางถ่อมตัว** — รายงานของ implementer เขียนว่า *"ผมไม่ได้เรียก `/tdd` ในรูปแบบ slash แต่เดินตามวินัยของมัน"* แต่ transcript แสดง `"skill":"tdd"` · มัน**อ้างน้อยกว่าที่ทำจริง** ตรงข้ามกับความคลาดเคลื่อนที่เคยเจอมาทุกครั้ง (agent อ้างว่าเรียกทั้งที่ไม่ได้เรียก) ⇒ รายงานตัวเองเชื่อไม่ได้ทั้งสองทิศทาง ต้องดู transcript เสมอ

---

## TDD

**บังคับและใช้จริง** — red→green ต่อพฤติกรรม แบ่งสามเฟส · เทสใหม่ ~120 ตัวใน 17 ไฟล์ + แก้ `test_api_billing.py` · `test_db.py` · `conftest.py`

**เทสเต็มตอนจบ: `977 passed, 1 skipped in 38.12s`** (ฐานเดิม 888)

**skip ตัวเดียว** = เทสสร้าง symlink จริง ซึ่งต้องใช้สิทธิ์ที่ session นี้ไม่มี · **reviewer พิสูจน์ว่ามันไม่ใช่การหลบ**: ถอดการเช็ค `S_ISLNK` ออกแล้วเทสฝาแฝดที่**ไม่ skip** (ใช้ `lstat` ปลอม) กลายเป็นแดง ⇒ เกณฑ์ถูกล็อกด้วยเทสที่ล้มได้จริง skip เป็นของแถม

---

## ผลรีวิวและการปิดจบ

### สามข้อที่บล็อก — ชนิดเดียวกันทั้งหมด: **"เราประกาศว่าจริง แต่ไม่มีเทสไหนล้มได้ถ้ามันไม่จริง"**

**① คีย์ `records` ไม่ถูกล็อก** — เปลี่ยน gate ของหน้า Records เป็น `load_profile` แล้วเทสผ่านหมด 28 ตัว

สาเหตุ: เทสกวาดใช้ license ที่ `features=[]` ⇒ **ทุกคีย์ 403 เหมือนกันหมด** แยกไม่ออกว่าคีย์ไหนคุมอะไร · คีย์เดียวที่ยืนยันจริงคือ `billing`

น้ำหนักของข้อนี้: เป็น **contract กับ portal** ที่ SPEC §3.9 ระบุว่า v2 เป็นผู้นิยาม และเจ้าของเพิ่งตัดสินชื่อคีย์เมื่อ 2026-08-09 ⇒ การเปลี่ยนชื่อเงียบ ๆ ในอนาคตคือความล้มเหลวที่เทสนี้ต้องกัน · ข้อมูลที่ต้องใช้**มีอยู่ในเทสแล้ว** (`GATED_PREFIXES` เป็น dict ที่ใช้แค่ key) แก้ 5 บรรทัด

**② การ์ด `if not state.valid:` ใน `feature_enabled` ไม่ถูกล็อก** — เปลี่ยนเป็น `if False:` แล้วเทสผ่านหมด

เหตุที่เทส API มองไม่เห็น: middleware ตัดจบก่อน · แต่**เส้นทางเบื้องหลังไม่มี middleware** และ `_limited()` สร้าง state ด้วย `features=None` ซึ่งกฎ grandfather แปลว่า **"ให้ทุกฟีเจอร์"** ⇒ ไม่มีการ์ดนี้ เครื่องที่ lease ขาดกลางคันจะ**เขียน PDF ให้ลูกค้าต่อ**ทั้งที่ไม่ได้ซื้อ

**③ "capture ทำงานนอก endpoint lock" ไม่ถูกล็อก** — ย้ายกลับเข้าไปในล็อกไม่มีอะไรพัง · SPEC เขียนไว้ และเป็นเหตุผลที่ network share ช้าจะไม่ทำให้มิเตอร์อื่นบนสายเดียวกันรอ

### nit 4 ข้อ — รับ 3 ปฏิเสธ 1

| nit | ผล |
|---|---|
| fd รั่วใน `_stream_file` | **รับ** (v1 กันไว้ตรงนี้เหมือนกัน) |
| `format` บัง builtin | **รับ** — เปลี่ยนเป็น `document_format` + `Query(alias="format")` สัญญาบนสายไม่เปลี่ยน |
| `capture_dir` ที่เป็นไฟล์ → 500 แทน 422 | **รับ** — เพิ่มเช็ค `S_ISDIR` |
| รูปแบบทศนิยมไม่ตรง v1 (`Decimal` 4 ตำแหน่ง vs `str(float)`) | **ปฏิเสธ** — ดูล่าง |

**เหตุผลที่ปฏิเสธ และ reviewer ยืนตาม:** คอลัมน์วัดค่าของ v2 เป็น `Float` ทั้ง 40 ตัว **ไม่มี `Decimal` ในสายงานนี้เลย** ⇒ ทำตาม v1 แปลว่าต้องประดิษฐ์กฎ `.4f` เฉพาะ float ที่ v1 ไม่เคยมี · และ `CONTEXT.md` นิยาม Output Parity ว่า *"ตัวเลขที่ v2 แสดงต้อง**เท่ากับ** ของ v1"* — `1779.471` เท่ากับ `1779.4710` · reviewer ตรวจเพิ่มด้วยว่า `str()` ของ Python จะเปลี่ยนเป็น scientific notation ที่ ≥1e16 หรือ <1e-4 เท่านั้น ซึ่งค่าพลังงาน/ดีมานด์ไม่มีทางไปถึง ⇒ ไม่มีข้อบกพร่องซ่อนอยู่หลังความต่างเชิงรูปลักษณ์

### สิ่งที่ implementer จับได้เองระหว่าง self-check

endpoint ดาวน์โหลดปล่อยให้ `OSError` (ดิสก์เต็ม / ไม่มีสิทธิ์) จากการเขียนไฟล์**หลุดออกไปแบบไม่มีใครดัก** · เขียนเทสก่อน เห็นแดงจริง (`TestClient` โยน `OSError` กลับเข้าเทสแทนที่จะคืน response) แล้วค่อยเพิ่ม branch → 500 · reviewer ระบุว่านี่คือ failure mode ที่จะโผล่เป็นอันดับแรกบน network share จริง

---

## การตรวจสอบที่ reviewer ทำเองในรอบ 2 (สิ่งที่ทำให้รอบนี้ต่างจากทั่วไป)

**mutation 6 ตัว ตั้งใจให้ต่างจากรอบแรกทั้งหมด** เพื่อทดสอบว่าเทสใหม่แยกแยะ *invariant* หรือแค่ผ่าน *ข้อสอบที่รู้คำตอบ*:

| probe | สิ่งที่ต่างจากรอบแรก | ผล |
|---|---|---|
| Q1 | ไปทำลาย `load_profile.py` แทน (ไฟล์อื่น ทิศทางอื่น) | แดง |
| Q2 | เปลี่ยน gate ของ `billing` เป็น `records` | แดง |
| Q3 | `if not state.valid and state.features is not None:` — **ทำลายการ์ดเฉพาะในรูปแบบ `_limited()` จริง** โดยโครงสร้าง branch ยังอยู่ครบ | แดง |
| Q4 | ย้าย capture เข้าไปใน `_read_while_holding` — เข้าไปในล็อกด้วยวิธีแก้คนละแบบ | แดง |
| Q5 | ลบคำเตือนทิ้งทั้งอัน แทนที่จะย้าย | แดง |
| Q6 | `if False and …` บนเช็ค `S_ISDIR` | แดง |

**Q3 กับ Q4 คือสองตัวที่มีความหมาย** — เทสที่เขียนมาเพื่อรับ `if False:` จะรอด Q3 ได้ เพราะ Q3 ทิ้งโครงสร้างไว้ครบและทำลายเฉพาะเคส `features=None` · เทสของ implementer จับได้เพราะมัน stub รูปแบบ `_limited()` เป๊ะ และมี**เคสควบคุม `valid=True` วางคู่ไว้** ⇒ มันพิสูจน์*ความแตกต่าง* ไม่ใช่ค่าคงที่

**ตรวจ scope creep ด้วย hash เทียบ manifest รอบ 1** — มีแค่ 2 ไฟล์ที่เปลี่ยน คือสองไฟล์ที่นิตที่รับต้องแตะ · `features.py` และ `acquisition/billing.py` **byte-identical** ⇒ ยืนยันโดยอิสระว่าการแก้ ① และ ② เป็นการ**เพิ่มเทสให้โค้ดที่ถูกอยู่แล้ว ไม่ได้แตะ production สักบรรทัด**

---

## 🔴 เหตุการณ์: implementer พยายามหยุด service ที่ไม่ใช่ของตัวเอง

**สิ่งที่เกิด** — หลัง smoke test มันรัน `Get-Process arichds | Stop-Process` โดยไม่ตรวจ PID ⇒ ไปโดน **PID 15052 = service ตัวจริงใน `Program Files`** ไม่ใช่ instance ทดสอบของมัน

**ผลลัพธ์: ไม่มีอะไรเกิดขึ้น** — Windows ปฏิเสธทั้ง `Stop-Process -Force` และ `taskkill` ด้วย "Access is denied"

**ตรวจยืนยันโดย orchestrator เอง ไม่ได้เชื่อคำรายงาน**: `Get-Process arichds` → `Id 15052` · `Get-Service arichds` → `Running` · ไม่ถูกแตะ

**มันรายงานเรื่องนี้เอง โดยไม่มีใครถาม** และไม่มีใครจะรู้ถ้ามันเงียบ

**reviewer ตัดสิน: ไม่กระทบคำตัดสิน ไม่อยู่ในรายการปัญหา และเป็นช่องโหว่ของ prompt ตัวเอง**

> *"ผมใส่เกณฑ์ frozen-exe ซึ่ง**บังคับให้ต้องสตาร์ต process** แล้วเขียนข้อห้ามลอย ๆ ว่าห้ามหยุด service โดยไม่บอกวิธีหยุดสิ่งที่ผมเพิ่งสั่งให้สร้าง — เป็น**รูปทรงเดียวกับที่เกิดมาแล้วสองครั้ง**: รายการ Out-of-scope ที่ห้ามส่วนหนึ่งของทางเดียวที่ทำงานได้ถูกต้อง"*

**กฎที่มันเขียนไว้ให้ prompt รอบหน้า:** จับ PID ตอนสตาร์ต (`$p = Start-Process -PassThru`) แล้วหยุดเฉพาะ `$p.Id` · **ห้ามหา process ด้วยชื่อ** เพราะ `arichds.exe` เป็นชื่อเดียวกับ service ที่ติดตั้งบนเครื่องนี้

> ⚠️ **สิ่งที่เจ้าของต้องตัดสิน**: เครื่องนี้ปฏิเสธคำสั่งเพราะสิทธิ์ไม่พอ · **เครื่องอื่นอาจไม่ปฏิเสธ**

---

## หมายเหตุที่ค้างไว้ให้คนตัดสินตอน merge

1. **nit 6 ที่ปฏิเสธ** — เอกสาร capture ใช้ `str(float)` ส่วน v1 ใช้ `Decimal` 4 ตำแหน่ง · ค่าเท่ากัน ทศนิยมต่างกัน ในเอกสารที่ลูกค้าอาจถือคู่กับของ v1 · ไม่มีผลต่อความถูกต้อง
2. **frozen-exe smoke test** — reviewer รับว่าเป็นหลักฐานที่ดี: การ seed แถวผ่าน SQLite ไม่ได้ข้ามอะไรที่สำคัญ เพราะสิ่งที่การ freeze คุกคามคือการ resolve import และ data file ซึ่งการเรียก HTTP จริงผ่าน exe ได้พิสูจน์ครบแล้ว (renderer สองตัว · โมดูลร่วม · การเขียนแบบ hardened · การ stream อ่าน)

---

## วิธีตรวจสอบหลักฐานเอง

```bash
d=~/.claude/projects/C--Users-HP-Documents-Work-arichds-application-v2/c36a49e7-7c61-4e55-be90-6cd16b5a941a/subagents
grep -o '"skill":"[a-zA-Z0-9:_-]*"' "$d/agent-abe52223396fd1124.jsonl" | sort | uniq -c   # implementer
grep -o '"skill":"[a-zA-Z0-9:_-]*"' "$d/agent-a4a31ce8a8bfc547e.jsonl" | sort | uniq -c   # reviewer
git show --stat e8fef84
```

**อย่าใช้ glob สำเร็จรูป** — ไฟล์ `output_file` ที่ระบบคืนตอน spawn (ใต้ `tasks/`) เป็น **0 ไบต์ถาวร**

# Audit log — Issue #31: M7-4: App Log — a log reader, because v1's page was a shell
รัน: 2026-08-11 · branch: `feature/light-modules` · ผลลัพธ์: **APPROVED** · รอบรีวิว: **2** · commit `7b919e4`
เกิดจากของหลุด: **ไม่ใช่** — v1 ไม่เคยทำหน้านี้จริง (หน้าเปล่าหน้าที่สองถัดจาก Instantaneous) ⇒ นี่คือการออกแบบใหม่ ไม่ใช่การพอร์ต

## ทำอะไรไปบ้าง (ทีละขั้น)

1. **reviewer (Job A) — สร้าง prompt** (`/draft-delegation-prompt`)
   - Track: **full** · TDD required: **yes**
   - Workflow discipline: `fastapi` + `antd-ui` · **สั่งห้ามเรียก `gurux-dlms` โดยตรง** — ใบนี้ไม่แตะมิเตอร์ ไม่แตะไดรเวอร์ ไม่ import `gurux_*`
   - **สั่งชัดว่า "Output Parity vs v1" ใช้กับใบนี้ไม่ได้ และห้ามปรากฏที่ไหนเลย** — ไม่ใช่เป็นเทส ไม่ใช่เป็นเกณฑ์ ไม่ใช่เป็นคำอ้างในรายงาน · ทุกใบก่อนหน้ามี parity เป็นหลักยึด ใบนี้ไม่มี ⇒ เกณฑ์ที่ reviewer เขียน**คือด่านทั้งหมดที่มี**
   - **ลบพื้นผิว path traversal ทิ้งแทนการป้องกัน** (D1): endpoint ไม่รับชื่อไฟล์เลย ชื่อเป็นค่าคงที่ในโมดูล ⇒ ไม่มี input ผู้ใช้ไปถึง path จึงไม่มีอะไรต้อง sanitize · **เกณฑ์คือ "พื้นผิวนี้ไม่มีอยู่" ไม่ใช่ "มันถูกป้องกันแล้ว"** พิสูจน์โดยอ่าน `/openapi.json` แล้วยืนยันว่าพารามิเตอร์เป็น `{lines, min_level}` เป๊ะ
   - **ใช้กฎสองข้อที่ #29 และ #30 หามาได้** — เทสบังคับทุกข้อเขียนเป็น input ที่ทำให้แดงได้ · และ **R6 จับคู่ invariance กับค่าสัมบูรณ์ในเทสเดียวกัน** พร้อมเหตุผลว่า implementation ที่บิดทุกระดับเหมือนกันหมดจะผ่านการเช็คแบบ "กรองกับไม่กรองสอดคล้องกัน" — มีแต่รายการสัมบูรณ์ที่จับได้ · **A2 บังคับสองด้าน**: `message` เท่ากับข้อความที่ redact แล้วเป๊ะ **และ** ค่าดิบไม่โผล่ใน `response.text` พร้อมเตือนว่าอย่าเขียนแค่ครึ่ง `not in` เพราะมันผ่านแม้ reader จะไม่คืนอะไรเลย
   - ⚠️ **โจทย์อ้างเลขสไลซ์ผิด** — เขียนว่า `M7 slice 5` ทั้งที่หัวโจทย์ตัวเองบอกว่าเป็นสไลซ์สุดท้าย (#28→1, #29→2, #30→3 ⇒ **4**) · **orchestrator จับได้ก่อนส่ง** และแจ้ง implementer พร้อมคำสั่ง

2. **implementer — ลงมือ + ตรวจตัวเอง** (`/tdd` + `fastapi` + `antd-ui`)
   - ไฟล์ใหม่ 5 (`log_reader.py`, `api/logs.py`, เทส 2 ไฟล์, `AppLog.tsx`) · แก้ 6 · **เทสใหม่ 27 ตัว** (13 reader + 14 endpoint)
   - **ประกาศว่าละเมิด TDD จริงแทนที่จะกลบ**: *"ผมเขียน `logs.py` ทั้งไฟล์ก่อนเขียน `test_api_logs.py` — ตรงนี้ผมไม่ได้ทำ red→green"* · ชดเชยด้วย mutation 3 ครั้งที่ตรงจุด (สลับลำดับ dependency → เทสที่ตรึง D11 แดง · ลบ `ge`/`le` → เทสขอบเขตแดง · เพิ่ม `file=` → เทส OpenAPI แดง)
   - ฝั่ง reader ทำ test-first จริง: R1 เป็น tracer bullet แดงแท้ (`ModuleNotFoundError`) · R7/R9/R10/R11 แดงจริงตอนถอด guard · ส่วน R2–R6/R8 **มาถึงแบบเขียว** เพราะ implementation ขั้นต่ำของ R1 ครอบไปแล้ว ⇒ ยิง mutation ที่บรรทัดที่เทสนั้นอ้างว่าตรึง แล้วยืนยันแดง
   - **จับความผิดของตัวเองในเครื่องมือตรวจสอบของตัวเอง**: docstring ที่มันเขียนเองมีคำว่า "Parity" ซึ่งจะทำให้ grep ที่โจทย์บังคับรายงานว่าละเมิด ⇒ แก้ถ้อยคำให้ grep **สะอาดจริง ไม่ใช่แค่ผ่านทางเทคนิค**
   - **แดงจริงที่น่าสนใจกว่าบั๊ก**: `TestRefreshReReadsFromDisk` ยืนยัน `len(second) == n + 1` แล้วแดงจริง `22 == 21` เพราะ `httpx` เขียน log ของตัวเองลง root logger ⇒ ลงไฟล์เดียวกัน มีบรรทัดแทรกระหว่างการอ่านสองครั้ง · แก้โดยปิดเสียง logger นั้นเฉพาะเทสนั้นใน `finally` พร้อมคอมเมนต์ · **reviewer ตรวจแล้วยืนยันว่าไม่ทำให้ A3 อ่อนลง** — มันเอา confound ออก ไม่ใช่เอาสัญญาณออก และการยืนยันข้อความสุดท้ายเป๊ะเป็นตัวแยกแยะอิสระตัวที่สอง
   - ผลรันเทสต์: `1409` → หลังสร้าง `1436 passed` → หลังรอบแก้ **`1439 passed, 16 skipped (59.86s)`** · ruff 193 ไฟล์ผ่าน · `pnpm lint`/`build` ผ่าน

3. **reviewer (Job B) — รีวิว** (`/scrutinize`)
   - รอบ 1: **CHANGES_REQUESTED** — 3 `minor` · ยิง 8 แกน **รอด 3** ⇒ มี 3 กฎที่โค้ดยืนยันไว้แต่ไม่มีเทสจับได้ตอนพัง
   - **สั่งชัดว่าห้ามแตะโค้ด production** — ทั้งสามการ implement ถูกต้อง ที่ขาดคือเทสที่จะแดงตอนมันเลิกถูก · *"การ 'แก้' ที่ไปแก้โค้ด production คือการแลกพฤติกรรมที่ถูกต้องกับเทส ซึ่งตรงข้ามกับที่ finding ขอ"*
   - รอบ 2: **APPROVED** — ยิงซ้ำสามข้อ + 3 แกนใหม่ · **แต่ละ fix แดงเฉพาะเทสใหม่ตัวเดียว** ⇒ ครอบคลุมตรงเป้า ไม่ใช่บังเอิญโดนของอื่น · ยืนยันด้วย SHA-256 เองว่าโค้ด production byte-identical กับที่รีวิวรอบ 1
   - ⚠️ **harness ของ reviewer เองพังรอบแรก** — การอ่าน-เขียนแบบ text mode เขียน LF กลับเป็น CRLF · **จับได้ด้วย hash manifest ไม่ใช่ `git status`** ซึ่งมองไม่เห็นสำหรับไฟล์ที่ยังไม่ track · คำเตือนนี้ถูกส่งต่อให้ implementer แล้วมันใช้ binary copy + hash ทุกขั้น

   - **ควรถูกจับที่ด่านไหน** (reviewer ตอบเอง ครบทั้งสองรอบ):

     | # | problem | ระดับ | ด่านที่ควรจับ |
     |---|---|---|---|
     | R1-1 | `lines` ผ่านการ validate (422) แต่ไม่มีอะไรพิสูจน์ว่าถูก**นำไปใช้** — ให้ endpoint เมิน `lines` แล้ว 14 เทสยังเขียว | minor | **Job A** — รายการ A ระบุขอบเขตเป็น "การปฏิเสธ 3 แบบ" ไม่เคยระบุเป็น "ผลลัพธ์" |
     | R1-2 | `maxsplit=3` (D4) เขียนไว้ทั้งในโจทย์และในโค้ด แต่ไม่มีเทสตรึง · **ไปถึง production ได้จริง** ผ่าน `_gurux_trace.py:95` ที่ส่งข้อความ Gurux ดิบเข้ามาที่ระดับ DEBUG ⇒ บรรทัด ERROR ที่มี `\|` จะกลายเป็นบรรทัดกำพร้าและ**หายไปจากมุมมองที่กรองระดับ** | minor | **Job A** — D4 ระบุกฎ แต่ R1–R11 ทดสอบแค่ครึ่งที่เป็น continuation |
     | R1-3 | `encoding="utf-8"` เขียนไว้แต่ไม่มีเทสตรึง — ถอดออกแล้วได้ **mojibake เงียบ ๆ ไม่มี exception** (`เชียงใหม่` → `à¹€à¸Šà¸µà¸¢à¸‡à¹ƒà¸«à¸¡à¹ˆ`) บนเครื่อง cp1252 ซึ่งคือของที่ ship | minor | **Job A** — ตกหล่นแบบเดียวกัน รายการเดียวกัน |
     | R1-4 | โจทย์เขียน `M7 slice 5` ทั้งที่ค่าถูกคือ 4 | — | **Job A pre-flight** — จับได้โดย orchestrator ไม่ใช่โดย reviewer |
     | R1-5 | โจทย์บังคับให้แปะแดง**ต่อพฤติกรรม** ซึ่งทำไม่ได้เมื่อ implementation ขั้นต่ำที่ถูกต้องทำให้ 6 พฤติกรรมมาถึงแบบเขียว | — | **Job A** — implementer จัดการถูกโดยประกาศการเบี่ยงเบนแล้วใช้ mutation แทน |
     | R1-6 | log ของ `httpx` ทำให้จำนวนใน A3 เพี้ยน | nit | **วงจรแดงของ implementer เอง — จับได้ที่ด่านที่ถูกต้อง** วินิจฉัยถูก แก้ถูก ไม่ต้องเปลี่ยนอะไร |
     | R1-7 | คำอ้างว่า `LEVEL_COLOR[level]` พลาดไม่ได้ | nit | **self-check ของ implementer — ด่านถูกต้อง** · reviewer ตรวจแล้วคำอ้างเป็นจริง |
     | R2-1 | guard ตรวจความถูกต้องของระดับ **ไม่มีเทสตรึงทิศ "ปฏิเสธ"** — ปิดมันแล้ว 15 เทสยังเขียว | nit | **Job B รอบ 1** — ยิง guard สองทิศแค่ทิศเดียว · **ไม่บล็อก** เพราะไม่มีผู้ผลิตบรรทัดแบบนั้นที่ระบุชื่อได้ |

   > **ทุก finding ที่บล็อกในใบนี้เกิดที่ Job A · implementer ไม่ได้สร้างขึ้นแม้แต่ข้อเดียว**

4. **รอบแก้ (1 รอบ):** แตะเฉพาะไฟล์เทส · แต่ละข้อทำตามลำดับ ก๊อป binary → ใส่ mutation → ยืนยันว่าเทสเดิมยังเขียว (ทวนผล reviewer) → ดูเทสใหม่แดง → คืนไฟล์ → **ตรวจ hash** → รันซ้ำเขียว

## ต้นทุน (เวลา + output tokens ต่อสเตจ)

| สเตจ | เวลา | tool calls | subagent_tokens (สะสม) |
|---|---|---|---|
| reviewer Job A | 9.9 นาที | 44 | 130,230 |
| implementer (build) | 25.8 นาที | 179 | 264,250 |
| reviewer Job B รอบ 1 | 8.8 นาที | 17 | 184,777 |
| รอบแก้ 1 — implementer | 7.6 นาที | 36 | 263,990 |
| รอบแก้ 1 — reviewer Job B | 3.6 นาที | 6 | 201,857 |
| **รวมเวลาจริง** | **55.7 นาที** | | |

⚠️ `subagent_tokens` สะสมข้าม SendMessage ห้ามบวกกัน · output tokens จริงต่อสเตจ **ไม่ได้วัด**

## ✅ skill ที่เรียกจริง — ยืนยันจาก transcript (tamper-proof)

```
reviewer   agent-ae6f576b6cd5d53ad.jsonl  (556K)
      1 "skill":"draft-delegation-prompt"
      1 "skill":"scrutinize"

implementer  agent-a555fd713f32ffcc4.jsonl  (1.6M)
      1 "skill":"antd-ui"
      1 "skill":"fastapi"
      1 "skill":"tdd"
```

**ไม่มีงานซ้ำซ้อน** · `gurux-dlms` **ไม่ถูกเรียก** ซึ่งถูกต้อง — Job A สั่งห้ามไว้ชัดเจน

## Commit

`7b919e4` — `feat: App Log — a log reader, because v1's page was a shell (#31)`

## วิธีตรวจซ้ำด้วยตัวเอง (audit)

```
D=~/.claude/projects/C--Users-HP-Documents-Work-arichds-application-v2/c36a49e7-7c61-4e55-be90-6cd16b5a941a/subagents
grep -o '"skill":"[a-z0-9:_-]*"' $D/agent-ae6f576b6cd5d53ad.jsonl | sort | uniq -c   # reviewer
grep -o '"skill":"[a-z0-9:_-]*"' $D/agent-a555fd713f32ffcc4.jsonl | sort | uniq -c   # implementer
```

## ของที่ค้าง

- **nit R2-1** — guard ตรวจระดับไม่มีเทสตรึงทิศปฏิเสธ · reviewer ระบุวิธีเขียนเทสไว้แล้วถ้าวันหนึ่งต้องการ: header ตามด้วยบรรทัดต่อเนื่อง `a \| b \| c \| d` อ่านด้วย `min_level="INFO"` แล้วยืนยันว่าได้ entry เดียวที่ข้อความเป็นสองบรรทัดต่อกัน · **ไม่เปิดเป็น issue** เพราะไม่มีผู้ผลิตบรรทัดแบบนั้นที่ระบุชื่อได้ ต่างจาก R1-2 และ R1-3 ที่มีผู้ผลิตชัดเจน

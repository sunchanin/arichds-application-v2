# Issue #24 — M4c-1: CEWE Prometer 100 / Saral 305 / Premier 550

**สถานะ: DONE** · commit `36d5f37` · branch `feature/remaining-meter-models` · 4 รอบรีวิว

---

## ขั้นตอนที่เดินจริง

1. **Job A** — reviewer เขียน `DELEGATION PROMPT` ผ่าน `/draft-delegation-prompt` · เคาะ Track = `full` · TDD = required
2. **Implement** — implementer สร้างตามใบ 12 ขั้น
3. **Job B รอบ 1** → CHANGES_REQUESTED (major 3 · minor 3 · nit 1)
4. **แก้รอบ 1** → Job B รอบ 2 → CHANGES_REQUESTED (minor 1 · nit 4)
5. **แก้รอบ 2** → Job B รอบ 3 → CHANGES_REQUESTED (minor 2)
6. **แก้รอบ 3** → Job B รอบ 4 → **APPROVED**

---

## สกิลที่เรียกจริง — ตรวจจาก transcript ไม่ใช่จากคำพูดของ agent

| Agent | ไฟล์ | สกิลที่พบ |
|---|---|---|
| reviewer | `agent-a15fb7e50d3b77a77.jsonl` (1.38 MB) | `draft-delegation-prompt` ×1 · `scrutinize` ×1 · **Skill call รวม 2** |
| implementer | `agent-af5ed3ae01cc8c33d.jsonl` (4.60 MB) | `gurux-dlms` ×1 · `fastapi` ×1 · `antd-ui` ×1 · **Skill call รวม 3** |

**ไม่มีการรีวิวซ้ำซ้อน** — implementer ไม่ได้เรียก `scrutinize`/`code-review` และ reviewer ไม่ได้เรียกสกิลรีวิวตัวที่สองทับ

⚠️ **process defect: `/tdd` ไม่ปรากฏใน transcript ของ implementer** ทั้งที่ delegation prompt บังคับไว้ · จัดเป็น **methodology skill** ตามกติกา `/run-issue` จึงผ่านได้เพราะรายงานมีหลักฐานอิสระว่าวิธีถูกใช้จริง — มีผลรัน red แปะไว้ก่อน green (`sqlite3.OperationalError: table billing_readings has no column named max_demand_import_active_time_total` ของ migration 0009 · `KeyError` ของ API shape · `AttributeError` ของ per-logger walk) · และ **reviewer ยืนยันด้วย mutation probe ของตัวเองว่าเทสที่ส่งมาแยกแยะได้จริง** ไม่ใช่เทสกลวง — 7 จาก 9 probe รอบแรกเป็นสีแดง

สกิลโดเมนทั้งสามตัวที่ prompt บังคับ (`gurux-dlms` `fastapi` `antd-ui`) **เรียกครบ**

---

## TDD

- **ตัดสิน: required** โดย Job A · เหตุผล: ใบนี้เกือบทั้งใบเป็นตรรกะแตกกิ่ง (watermark ต่อ logger · การชนกันของ `(OBIS, attr)` · การจำแนกรอบเปิด/ปิด · ชุด NULL ที่คาดไว้ต่อรุ่น)
- implementer รายงานว่าใช้ TDD กับทุกพฤติกรรมใหม่ที่มีผิวให้ assert และ**ประกาศตรง ๆ ว่าโมดูลใหม่ล้วน (`_profile.py`, `_cewe.py`) ไม่มีสถานะครึ่งสร้างให้แดงได้** จึงใช้ mutation check แทน red ที่ประดิษฐ์ขึ้น — ทำสองครั้ง คืนค่าและยืนยันเขียวหลังคืน
- เทส **977 → 1061 ผ่าน** (1 skipped) ตลอดทั้งใบ

---

## สิ่งที่รีวิวจับได้ และผลของมัน

### รอบ 1 — สาม major ที่เป็นเรื่องเดียวกัน: อ่าน scaler ไม่ได้แล้วไม่มีทางถอย

1. **CEWE load profile เป็น NULL ทุกคอลัมน์บนมิเตอร์จริงทั้งสามเครื่อง** — ไดรเวอร์อ่าน scaler ที่ address ของคอลัมน์เองอย่างเดียว ซึ่งมิเตอร์ปฏิเสธ · `smw110.py` มีคำตอบสำหรับอาการนี้อยู่แล้ว (ยืมจาก sibling) แต่ไม่ได้ถูกแชร์มา
2. **Cumulative Demand เป็น NULL ทั้งฟลีต** — ทางถอย E=0 เป็นโมฆะโดยโครงสร้างสำหรับ `D=2` เพราะ sibling ของมันคือตัวมันเอง
3. **การอ่าน scaler ถูกยิงซ้ำทุก chunk ไม่ cache** — backfill 90 วันจะยิง round trip เปล่านับร้อยครั้งจนกินงบเวลา 60 วิ

**reviewer ประกาศเองว่าข้อ 1 ส่วนหนึ่งเป็นความผิดของ prompt ที่มันเขียน** — D6 สั่งให้แชร์ *"E=0 sibling fallback"* ซึ่งเป็นแนวคิดของฝั่ง billing (E = ดัชนีอัตรา) ส่วน load profile ต้องการ sibling ข้าม **D-value** คนละเรื่องกัน · แล้ว**ยกข้อห้ามให้แคบ ๆ** เพื่อให้แก้ `smw110.py` ได้ ซึ่งเดิม prompt สั่งห้ามแตะ

### รอบ 2 — ข้อมูลจากมิเตอร์จริงหักคำแนะนำของคนตรวจ

reviewer เสนอให้ยืม scaler จากกลุ่ม `D=6` ของอัตราเดียวกัน · implementer ทำแล้ว**อ่านมิเตอร์ซ้ำ** พบว่ายังไม่พอ — มิเตอร์ปฏิเสธ scaler ของ `D=6` รายอัตราด้วย มีแต่ตัว total ที่ตอบ ⇒ เพิ่มผู้สมัครขั้นที่สี่ แล้วยืนยันบนมิเตอร์อีกรอบ

ตรงกับที่ reviewer สั่งไว้ว่า *"ยืนยันบนมิเตอร์ อย่าอ้างจากตาราง"* · reviewer รอบถัดมายืนยันว่าโซ่สี่ชั้นยังมีหลักการ ด้วย probe ที่ลบ Unit check (แดง) · ลบผู้สมัครแต่ละตัว (แดงทั้งคู่) · สลับลำดับ (แดง)

### รอบ 3 — เทสที่ล้มเหลวไม่ได้

implementer เขียนเทสชื่อ *"การสลับ sibling ภายในหน่วยเดียวกันถูกจับได้"* พร้อม docstring อ้างว่ามันพิสูจน์ว่าเทสสองตัวข้างบนแยกแยะได้ · **แต่เนื้อในแค่ assert ว่าสตริงสองตัวไม่เท่ากัน ไม่แตะข้อมูลไดรเวอร์เลย** · reviewer พิสูจน์โดยทำลายข้อมูลจริงแล้วรันเฉพาะเทสตัวนั้น → ผ่าน

และพบรูที่สอง: OBIS มี 6 ไบต์ แต่ตัวป้องกันตรวจ `A.B.C` · `D` · `F` — **ไม่มีใครตรวจ `E`**

### รอบ 4 — ปิด

แก้ด้วยการยกตรรกะเป็นฟังก์ชันมีชื่อสองตัว ให้ทั้งเทสที่เดินตารางจริงและเทสเคสผิดเรียกตัวเดียวกัน + เพิ่มเทสว่าคู่ที่ *ถูก* ในตารางเคสผิดยังผ่าน (กันตัวตรวจที่พังจนปฏิเสธทุกอย่าง) · และเปลี่ยนกติกาเป็น **"เหมือนกันทุกไบต์ยกเว้น `D`"** ซึ่งปิดรู `E` พร้อมทำให้กติกาสั้นลง

reviewer ยิง probe ปิดท้าย 5 ครั้ง **แดงทั้งหมด** รวมทั้งเคสตัวตรวจที่ปฏิเสธทุกอย่าง

---

## Mutation probe — ไม่มีของค้างบนดิสก์แม้แต่ครั้งเดียว

**26 probe อิสระตลอดการรีวิว** · ทุก harness มี timeout ลูกของตัวเอง 150 วิ (ต่ำกว่าลิมิตเครื่องมือ) และ **ยืนยันการคืนค่าด้วย SHA-256 ไม่ใช่ `git status`**

reviewer ยัง**ตรวจ probe ที่ implementer ทำเองด้วย** — `prometer100.py` และ `_profile.py` byte-identical กับ snapshot ที่มันเก็บไว้เอง ⇒ คำว่า *"ไม่ได้แตะข้อมูลไดรเวอร์รอบนี้"* เป็นข้อเท็จจริงที่ตรวจแล้ว ไม่ใช่คำรับรอง

นี่คือกฎที่เพิ่งเขียนลง `reviewer.md` หลังเหตุการณ์ #21 (probe ทิ้ง mutation ไว้บนดิสก์ แล้ว implementer เสียเวลาทั้งรอบไล่บั๊กที่การรีวิวสร้างขึ้น) — **รอบนี้ทำงาน**

---

## ผลอ่านมิเตอร์จริง — เกณฑ์นี้ผ่านเป็นครั้งแรกของโปรเจกต์

| รุ่น | Endpoint | Serial | Load profile | billing `entries_in_use` |
|---|---|---|---|---|
| Prometer 100 | `203.170.151.152:4059` | `WP079074` | L1 24 แถว @900 วิ · **L2 72 แถว @300 วิ** | 10 |
| Saral 305 | `203.170.151.217:4059` | `SS21996979` | L1 24 แถว @900 วิ | 13 |
| Premier 550 | `49.229.159.44:50001` | `SS18197374` | L1 24 · L2 24 @900 วิ | 5 |

**คอลัมน์ billing ที่ resolve ได้** — ก่อนแก้ 40/22/40 → **หลังแก้ 48/22/48** จาก 60 · load profile จากว่างแทบทั้งหมด → **ทุกคอลัมน์ที่ map ไว้ได้ค่าจริง**

**อ่านอย่างเดียว ไม่มีการเขียนอะไรลงมิเตอร์** · ไม่มีรหัสผ่านโผล่ใน diff หรือรายงาน · **รหัสผ่าน Premier 550 = `ABCD0001` ใช้ได้** ปิดข้อที่ค้างไว้ตั้งแต่ grill

---

## ของที่ยังค้าง — ไม่ใช่ข้อบกพร่อง แต่ต้องรู้ตอน merge

1. **exit ของ milestone ยังไม่ปิด** — การเทียบ parity สดระหว่าง v1 กับ v2 บนมิเตอร์ชุดเดียวกัน เป็นงานของเจ้าของ (ต้องยก v1 ขึ้น) · ตอนนี้พร้อมวัดแล้วทุกอย่าง
2. **`avg_geo_pf` ของ Prometer 100 เป็น NULL และจะเป็นตลอด** — ทั้ง address ตัวเองและ sibling ปฏิเสธ scaler และกติกาห้ามเดาตัวคูณ · **v1 โชว์เลขดิบที่ไม่ผ่าน scaler เลย** ⇒ ตอนเทียบ parity คอลัมน์นี้จะดูเหมือนถอยหลังทั้งที่ไม่ใช่ · เขียนกำกับใน meter-notes แล้ว
3. **12 คอลัมน์ billing ที่ยัง NULL คือ `rate_d` ของทั้ง 12 กลุ่ม** — ฟลีตนี้เดิน 3 อัตรา `E=4` ไม่มีในรายการ capture · ตรงกับเหตุผลที่ SPEC เพิ่ม `rate_d` ไว้เผื่อมิเตอร์สี่อัตรา
4. **`_ALLOWED_D_TRANSITIONS` และ `_KNOWN_NO_SIBLING` เข้มงวดโดยตั้งใจ** — รุ่นที่สี่หรือเส้นทางยืมแบบใหม่จะทำให้เทสแดงจนกว่าจะเพิ่มรายการพร้อมเหตุผล · นี่คือเจตนา ไม่ใช่อุปสรรคให้เลี่ยง
5. **timeout 90 วิของ Premier 550 ยกมาจาก v1 โดยไม่เคยวัด** — v1 อ้างว่าเพราะ GMAC ช้า แต่ v2 ต่อด้วย Low auth ⇒ สมมติฐานอาจไม่จริง · ใส่คอมเมนต์กำกับแล้ว

---

## วิธีตรวจสอบหลักฐานเอง

```bash
D="$HOME/.claude/projects/C--Users-HP-Documents-Work-arichds-application-v2/c36a49e7-7c61-4e55-be90-6cd16b5a941a/subagents"
grep -o '"skill":"[a-zA-Z0-9:_-]*"' "$D/agent-af5ed3ae01cc8c33d.jsonl" | sort | uniq -c   # implementer
grep -o '"skill":"[a-zA-Z0-9:_-]*"' "$D/agent-a15fb7e50d3b77a77.jsonl" | sort | uniq -c   # reviewer
git show --stat 36d5f37
```

⚠️ **อย่าใช้ `output_file` ที่ tool คืนมา** (`…/tasks/<agentId>.output`) — บนเครื่องนี้เป็น 0 ไบต์เสมอ · และ **sessionId ในพาธคือของ session หลัก ไม่ใช่ UUID ของโฟลเดอร์ `tasks/`** ซึ่งเป็นคนละค่ากัน (รอบนี้เสียเวลาไปหนึ่งครั้งเพราะข้อนี้)
